# 🔑 HANDOFF — Dacon 236694 인수인계 (단일 진실 소스)

> **이 문서는 에이전트 메모리와 독립적인 프로젝트 내 상태 기록이다. 새 채팅은 이 파일 → `실험로그.md` → `대회규칙_채점환경.md` 순으로 읽고 이어받는다. 중요 작업마다 이 문서를 갱신한다 (§10 프로토콜).**
> 최종 갱신: 2026-07-15 02:4x KST (campaign official-meta-wave1 후속 metaT 프레임 BLEND_W 스윕 post_lb PASS — 챔피언 META-T(0.60)→META-T_w062(0.62) 교체(near-zero), 블렌드가중 레버 챔피언 프레임서 사실상 소진으로 정정, 상세는 실험로그 2026-07-15 항목)

---

## 1. TL;DR — 현재 상태
- **★예선 종료(2026-07-15): 최종 챔피언 metaT_w062 = 0.7866974871, 본선 컷 0.79591 미달(−0.0092), 본선 진출 실패.** (최종 2제출: seedlotto_w062 0.7866826118·wmean_w062 0.786608523, 둘 다 챔피언 미달·near-zero delta — 상세는 실험로그 2026-07-15 "예선 종료" 항목.)
- **★신챔피언: META-T_w062(BLEND_W=0.62) = LB 0.7866974871 (2026-07-15, post_lb PASS, campaign official-meta-wave1 후속 BLEND_W 스윕).** 구성 = 직전챔피언 META-T와 동일 프레임·멤버·transcript(챔피언고전×1 + s48 + kf768_81 + kf768_metaT), **BLEND_W만 0.60→0.62**(seed0.62/classic0.38, within-frame 단일 스칼라 matched·재학습 없음·draw0). 배포 zip `submits/submit_metaT_w062.zip`(SHA 2bdd24e93a…). 직전챔피언 대비 **+0.0000437974**(near-zero — 결정론적 실차이이나 전략적으로 유의미한 개선 아님·"노이즈" 아님; 최고LB 자동채택 규칙상 신챔피언). w064=0.7865074247(vs w062 −0.0001900624, 과튜닝) → 곡선 argmax=0.62, 0.60–0.62 사실상 평탄. ⚠️**채점 600s=T4 한계 정각(마진≈0)**, w064 599s·metaT(0.6) 598s — metaT 프레임 추가 멤버·대형화 절대 불가. **blend-w 레버는 이 프레임에서 사실상 소진**(§6 정정). 직전 blendw 레코드의 "+0.0010 전이·~0.7877" 가설은 **실측 반증**(0.7866974871 ≪ 0.7877). 상세: 실험로그 2026-07-15 항목.
  - **(직전 챔피언) META-T(BLEND_W=0.60) = LB 0.7866536897 (2026-07-14 23:40, post_lb PASS).** 구성 = 챔피언고전×1 + s48 + kf768_81 + kf768_metaT(seed79 transcript-meta 증강 멤버). cA(0.7848461512) 대비 **+0.0018075385**, META-N(0.7850069558) 대비 **+0.0016467339**. 배포 zip `submits/submit_metaT.zip`(SHA ae0b71a2…). ★transcript-meta(공식메타를 transcript에 추가)가 numeric-meta보다 강함을 matched로 **시사**(단 n=1·draw경계 유보 — 강단정 금지). 재현성: metaT 멤버=colab 99331657 `META_TRANS_EXT=1` seed79 REFIT_ONLY m2b_s79 pretext, combo는 cA 원본 feat.py + 모델별 transcript(metaT만 확장).
  - **(직전 챔피언) cA = LB 0.7848461512 (07-13 23:53 제출, post_lb round2 PASS)** (구성: `챔피언고전×1 + s48 + kf768_81 + mint2b_s79`, 순수블렌드 0.6/0.4). 카드1(0.784156489) 대비 **+0.0006896622**, v58 대비 +0.0007696559. ⚠️**'k81 우위/조합 발견' 단정 금지**: cA↔카드1 차이는 kf seed79→81 draw 교체이고 seed-diversity 교락(카드1={79,79} vs cA={79,81})으로 k81 품질과 분리 불가. 배포판 `submits/submit_cA.zip`(SHA e09abe21…). 카드3 델타는 seed 스프레드 내라 balanced 자체효과 여전히 미분리.
  - (직전2) 카드1 = 0.784156489 = `고전 + s48 + kf768_79 + mint2b_s79`(submit_m2b79.zip c7180c41). v58 = 0.7840764953. cB(더블 mint) = 0.781756698 기각(약한 m2b81 draw + k79 손실 묶음 — 'mint 스태킹 죽음' 단정 금지).
  - **★스테이징 오염 주의: scratchpad/stage_cA~cF는 script.py 하드링크 공유로 전부 cF 내용 — 재zip 금지**(zip들은 빌드시점 정상, 검증됨). build_combos.py는 편집 전 하드링크 분리 필요.
- mint2v2-dualseed(v2 balanced-mint 보정, cA 프레임 mint2b_s79 슬롯 교체) 서버 실측 s79 −0.0017024368 / s81 −0.0004094500 → 둘 다 미달, cA 유지(2026-07-14 오후, post_lb PASS). ★'v2 축 죽음/단독효과' 단정 금지 — 4변수 묶음·2 draw·cA 프레임 한정.
- **campaign all-info-h100-final-0715(ALL-INFO=정확히는 'ALL-REAL+STABLE-MINT') 서버 실측, post_lb PASS: 3nn=0.7850171837(598s)/1nnc=0.7318299466(191s), 둘 다 챔피언 metaT_w062 미달(3nn −0.0016803034) — metaN 수준 안착(3nn vs metaN +0.0000102279), 챔피언 불변·ALL-INFO 미채택.** pretext·numeric채널·draw(A100) 동시변경 묶음이라 원인 미분리(negative, 단독효과 단정 금지). 상세: 실험로그 2026-07-15 항목.
- **⚠️채점 9분44초/10분 — 현 프레임 추론시간 포화(마진 16초).** 멤버 추가·대형화 불가, 개선은 시간중립 축만(멤버 품질·학습목적·데이터 정합성·고전측).
- **최종선택은 자동**(최고 LB 점수가 자동 채택) — 수동 라디오 없음. 신경 안 써도 됨.
- **순위: 0.7809 기준 36위였음(v47로 상승 추정, 확인 필요) / 4위 0.795 / 1위 0.83.** 4~36위 밀집 → **+0.001이 여러 계단**.
- **목표(2026-07-15 갱신): 12등 ref 0.79307(변동가능, 리더보드는 유동값) — 챔피언 META-T_w062(0.7866974871)에서 +0.0063725129 필요**(일관쌍 정정 — 07-15 metatsweep 감사 nonblocking finding. 직전 metaT(0.6) 기준 +0.0064163103·직전 cA 기준 +0.0082238488은 챔피언 교체로 갱신). 예선 마감 7/15. (구 표기 "본선컷 0.788~0.7906·+0.0039~+0.0065"는 낡음 — 외부 감사 정정.)
- **★재현성 리스크(외부 감사 지적): 챔피언 멤버 kf768_81은 학습 manifest(effective_config) 없는 v1 파이프라인 draw.** s48·kf768_79도 동일 계열. 본선 진출 시 재현성 자료 동결 전에 이 한계를 명시 문서화할 것.
- **★balanced-mint 해석 정정(07-14 외부 감사): mint2b 계열의 "1변수 clean" 주장은 과대 — 실제는 "rolling-window + 클래스가중(raw bincount :298)·배치정규화(wt.sum() :364) 변화의 묶음효과."** TARGET_BALANCE는 배치 구성 의존(재현서 target 기여 max/min ≈1.9배). 수정판 = mint2-balanced-v2(배치독립 가중손실·target가중 클래스가중·strict 6,829 window·안정필드 메타 통일) 진행 중.
- **제출: 하루 10회.** 07-12 10/10 소진 종결(챔피언 v58 유지). 07-13 리셋.
- 과제: AI 코딩 에이전트 세션 → 다음 행동 14클래스 예측, Macro-F1, **코드 제출형**(script.py가 숨겨진 test 서버서 실행).

## 2. 채점 환경 (배포 제약 — 위반 시 0점/타임아웃)
- 서버: **T4 16GB + 3vCPU/12GB RAM**, 추론 **10분(600초)** + 설치 10분 별도, **zip 1GB**, 오프라인.
- test = **세션당 1 step** 구조(로컬 스텁과 동일, v9서 확인). 세션 연속성 수확 불가.
- 필수: `net.half()` 명시(fp16 저장본 fp32 업캐스트 방지), requirements 버전 = pickle 생성환경과 일치(numpy==2.0.2 등), sklearn pickle의 RNG 제거.
- 상세: `open/docs/대회규칙_채점환경.md`. 본선: 7/20 자료제출, 7/24 코드검증(재현성 필수).

## 3. ★확립된 법칙 (전략의 근간)
1. **결정론**: 채점은 완전 결정론(동일파일 재제출 = 소수점 10자리 동일). **서버 = 무노이즈 오라클, 모든 제출이 진짜 측정.**
2. **입력 고유율 99.97%**: 라벨은 입력의 결정론적 함수. 0.83은 데이터천장 아님.
3. **정보축 vs 정규화축 (핵심 구분):**
   - **정보 추가(더 긴 컨텍스트·더 많은 **좋은** 데이터·refit·[SRC]·더 강한 멤버) → 서버로 전달됨** ✅ (단 데이터는 품질↑일 때만; mint2 저품질 증량은 v40서 −0.0021)
   - **정규화/재조합(FGM·증류·label smooth·specialist·스태킹·CA·블렌드가중튜닝) → 서버서 반납** ❌
4. **★★홀드아웃의 역할 = "제출 자격 필터"일 뿐, "우열 판단자"가 절대 아님 (사용자 명시 규칙):**
   - 홀드아웃 점수로 후보를 **"대박/당첨/최고"라고 부르지 말 것.** 홀드아웃이 높다 = 좋다가 **아님**.
   - 홀드아웃은 오직 **오차범위(±0.0058) 안에 드는지**만 판정 → 들면 "제출해볼 자격 있는 후보"일 뿐.
   - **실제 우열·챔피언은 LB로만 판단.** 어느 후보를 제출할지는 **전략**(정보축인가·기전이 살아있나·스택 가치)으로 결정, 홀드아웃 대소로 결정 금지.
   - 갭 통계(참고용): 현 레시피 **서버LB = 홀드아웃 + 0.0131 ± 0.0039**(반전보너스). 그래서 홀드아웃은 서버를 체계적 과소평가하고, 방향도 개별로는 안 맞음(v31 코어홀드 1위→LB 최하, FGM 홀드 최고→LB −0.0028). **⇒ 홀드아웃 순위 매기기 무의미, 밴드 통과 여부만.**
   - 제출 자격: **블렌드홀드아웃 ≥ (챔피언홀드아웃 − 0.0058) 이면 후보 자격.** 정보축이면 자격 충족 시 서버로 실측.
   - 예외: 정규화/specialist류는 서버증거(v24 −0.0035)로 이미 닫힘 — 홀드아웃 무관하게 재시도 금지.
5. **mint 행수 해석:** `mint2` 6,859행은 신규 상태 6,859개가 아니라 canonical missing step **3,180개**의 rolling-window 변형이다. 각 target의 최장 history는 항상 `mint1`; 동등가중 직접주입은 target별 1~6배 과대표집한다. 전역 exact text-state real 중복 18행을 뺀 **3,162행이 strict 고유 text/action supervision의 최대치**다.
6. **mint 메타 복원 감사(07-11):** 세션 불변 4필드와 `turn_index`는 3,180/3,180 정확. 전후 실제 row+관측 액션으로 CI 3,108, git_dirty 3,016, open_files 2,910을 정확 복원하며 strict-dedup 교집합은 **2,863행**이다. `budget_tokens_remaining`/`elapsed_session_sec`는 중간 시점 값이 관측되지 않고 선형복원이 각각 11/47,963·572/47,963만 일치해 exact로 간주할 수 없다.
7. **앙상블 필요성은 현재 세대에서 미확정:** 과거 RoBERTa 세대는 멤버 추가가 서버 `+0.0012~+0.0026`, classic 비중 유지가 `+0.0024`였으나, 현 v49의 k79/k78/s48에서 member-count 단독변수 서버 ablation은 없다. 공정 hidx에서 `k79+k78+대체 classic OOF=0.77186`이 `s48+k79+k78+대체 classic OOF=0.77163`보다 근소하게 높아 s48 필수 전제는 성립하지 않는다.

## 4. 우승 레시피 & 배포 구조
- **레시피 (v17 base2):** `[SRC] au|sim` 프리픽스 + 풀 트랜스크립트 직렬화(`[U]`/`[A] name args -> result`/`[META]`/`[P]`) + **MAX_LEN 512 + truncation_side="left"(필수)** + 메타 119d(StandardScaler) concat + sqrt 클래스가중 + 민트 증강(EXTRA_DATA).
- **HybridNet:** backbone CLS(768d) ⊕ meta 119d → Linear(256)+GELU+Dropout(0.1)+Linear(14).
- **배포 블렌드(v49 챔피언):** `argmax( 0.6×mean(s48, kf768_79, kf768_78) + 0.4×고전블렌드[0.45 HGB+0.40 LogReg+0.15 NB] )`. **결합기 없음(순수 블렌드)**. seed 미완주 시 가용seed만 평균→고전 순 자연강등. 데드라인 캐스케이드 170/370/575s.
- **배포 예산: zip 978.6MB decimal/1GB, ⚠️채점 9분44초/10분(마진 16초) — 시간이 지배 제약.** 멤버 추가·대형화·이중고전 전부 불가. 멤버 교체는 동급 이하 비용만.
- **kf 슬롯 = kf768 ×2 (seed79/78, kf-deberta-base + MAX_LEN 768).** seed79 교체는 서버 `+0.0011669`로 전달. ⚠️"768 장문맥 효과" 자체는 비A/B로 미증명 — 강한 멤버로만 취급.

## 5. ⚫ 죽은 축 (재방문 금지 — ★07-10 Codex 감사로 범위 정밀화: "정확히 그 구현"만 닫힘)
- **확실히 닫힘(서버 실측):** 멤버 셔플/블렌드w(0.779 포화, w=0.6 정점)(★구 RoBERTa/v49-era 프레임 한정 실측. meta 증강 프레임에선 BLEND_W 0.62>0.6이 +0.0010 매치드[metaN_w062 0.7860 vs metaN 0.7850, 2026-07-15] → §6 살아있는 시간중립 레버로 재분류. 구 실측이 틀린 게 아니라 frame-specific.) · dv3 다양성(v32 −0.0043) · CA prior(3연패) · **subset CE 스페셜리스트**(독립 부분집합 전용모델 — 공정 재계산서도 4형제 0.609<0.625, 추론군 0.674<0.723 열등) · **student4 전면교체 증류**(v26) · **mint2 직접주입**(v40·v43) · **relv2 12종 일괄주입**(v46) · **트랜스포머×복원민트 멤버**(v44 −0.0025, s51mf/kf768m2f 붕괴로 3중 확인) · roberta768(절대위치 보간손상) · large 단독구조(v23b) · 스태킹/결합기 튜닝(v21b 계열).
- **감사로 "미확정"으로 재분류(재시도는 matched control 필수, 우선순위 낮음):** FGM(비대조 1회, vanilla seed75 대조 없었음) · label smooth(1회) · session-equal 가중(1회) · k-NN/페어와이즈(baseline이 raw-logit 합성이라 무효 — 단 재계산서도 약했음).
- ⚠️구 기록 주의: v24 "+0.0124 홀드아웃"은 **refit 누수 측정으로 무효**(감사 §4.1). "홀드아웃 이득의 서버반납" 서사의 근거 아님.
- **mint CI none화 직접주입은 현재 구현 범위에서 종료:** v48의 `잘못된 CI none화 + zero-sentinel mask`는 v47 대비 `−0.0008415`, v50의 no-mask `cin74`는 `−0.0013033`. 따라서 `train_mint_ci`를 쓰는 transformer 직접주입/seed 확장은 재시도하지 않는다. 이는 strict-dedup text-only→real-only fine-tune 축을 닫지 않는다.
- **★07-13 사용자 확정 "사실상 닫힌 축"(재개 금지):** ①무작정 seed 증산 ②mint 직접 동등가중 주입 ③잘못 복원한 CI/zero-sentinel ④단순 MAX_LEN 1024/1536 ⑤FGM·specialist·stacking·class adjustment ⑥블렌드 가중치 재튜닝(frame-specific — meta 증강 프레임 제외, 거기선 살아있음) ⑦기존 방식의 대규모 앙상블 추가 ⑧Qwen 0.5B 현 형태(디코더·미캐시·INT8무툴 — deberta-v3-korean으로 피벗됨).
- **A/B/C 3KF 조합 = 파킹(닫힘 아님):** s48(512)→kf768(768) 순수 스왑이 zip 1GB·T4 600초 위반(zip≈1,116MB·시간마진 붕괴). 세 조합 전부 홀드아웃 밴드 통과했으나 **INT8/용량절감 툴링 확보 시에만 재개방**(§6 배포기술 축). 계산 `scratchpad/abc_band.py`.

## 6. 🟢 살아있는 축 (★07-13 사용자 우선순위 재확정 — 이 순서대로. 이전 07-10 목록 대체)
1. **Balanced mint2 rolling-window (현 주력, GPU 가동 중):** 같은 canonical target의 1~6개 history window 활용 · target별 총가중 동일화 · 복원 가능한 metadata만 · Stage A 3ep → real-only Stage B 6ep. **기존 mint2 직접주입 실패(§5 v40/v43)와 다른 구현.** 확인: seed79/81 서버 효과 · **mint1-exact matched control과 비교** · 유망하면 Stage A 6ep dose A/B. (밴드체크: cand_s79 홀드 0.7737 / cand_s81 0.7703, 둘 다 in-band — LB 대기.)
   - v2 보정판(mint2v2-dualseed-0714)은 cA 프레임 2 draw 모두 서버 미달(−0.0004~−0.0017) — 단일변수 death 아님(4변수 묶음), keep-시드 v2 증산 비주력.
2. **신규 백본 team-lucid/deberta-v3-base-korean (현재 가장 큰 미실험 카드):** 기존 kf-deberta와 다른 사전학습 · 한국어 인코더 · HybridNet 재사용 가능성 높음 · v58 k81 슬롯 교체 후보. 선행 게이트(전부 완료·§9 prep): fp16 zip 크기(≈886MB) · tokenizer 오프라인 배포(SPM 라운드트립 미실증) · head 입력 차원(887 동일) · T4 추론시간(estimated). **prep 완료 → pre_launch 감사 후 2seed.**
3. **수정 증류 (범위 제한 재실험, 과거 v26 전체 아님):** teacher = OOF74+OOF79+classic OOF · temperature 완전 일치 · ID/split/class-order 정렬 · hard CE 유지 · student 한 슬롯만 교체. **기대값 낮음 → 1회 probe 한정.** (teacher prep 완료: make_soft_target temp-match·조립검증.)
4. **Balanced-mint 내부 분리실험 (mint2b 결과 유망할 때만):** ①mint1 exact vs mint2 balanced = rolling-window 효과 ②keep vs omit = 정확 metadata 효과 ③Stage A 3ep vs 6ep = pretext dose 효과. **한 번에 변수 하나만 바꾸는 matched A/B.**
5. **신규 강멤버의 슬롯·조합 효과 (새 모델 나오면):** 같은 모델을 두 위치에서 — v58 k81 교체 · v58 k79 교체 · 당일 신규모델 최강 2종 동시 조합 · 신규모델 1NN+classic 진단. **k81 승리가 프레임 의존적이었기 때문에 필요.**
6. **고전 모델 개선 (이론상 중요, 서버 classic 기여 +0.0061~0.0062 최대 — 단 현재 구체적 강후보 없음):** 현재 classic artifact 정확 재현 상태에서 모델/정보 개선 · history 깊이 무관 관계형 피처 · 같은 추론비용 내 모델 교체. **relv2·relv3 기존 구현은 재사용 안 함.**
- **★transcript-meta(session_meta 공식필드를 [META] transcript에 직렬화) = 서버 전달 확인된 정보축**(metaT +0.0018 vs cA, 2026-07-14 밤 post_lb PASS). 확장 여지: 다중 seed·더 많은 메타필드·다른 백본과 결합. (numeric-meta[metaN]는 +0.00016로 약함.) ⚠️metaT 채점 598s/600s(마진 2s) — 확장 시 시간중립만 허용, 멤버 추가·대형화 불가.
- **관측 추가(법칙 아님, 묶음 — 2026-07-15 all-info-h100-final-0715):** "numeric-ext 125d를 transcript 멤버에 함께 적재(all-info 채널스택)" 방향은 서버서 metaN 수준으로 회귀 = **이득 없음(negative)**. pretext(stable-meta)·draw(A100)가 동시변경된 묶음이라 **죽은축 종결 아님**(인과 미분리) — 관측으로만 기록. 현재까지는 **단일채널 transcript(metaT) > 채널스택(ALL-INFO)** 관측. transcript-meta 자체는 챔피언축으로 유지.
- **★다양성(멀티시드/멀티백본 앙상블) = 확립된 최대 레버(2026-07-15 metaT 1NN+C 진단, post_lb PASS로 정량화):** 1→3멤버 = **+0.0530238797**(measured, matched BLEND_W 0.6, metaT 프레임 3NN→1NN). meta 정보축 +0.0018075385의 **29×**·블렌드가중 +0.0000437974의 **41×**. 단 +0.053은 **'앙상블을 갖는 것 자체'의 값**(1→2 멤버 전이가 지배 — cA-2NN LOO 개별합[s48 +0.0110543983 + k81 +0.0052044955=+0.0162588938]의 3.26배 초가법) → **신규멤버 1장으로 회수 가능한 값이 아님**(과대추정 금지). 3-멤버 margin에서 신규/교체멤버 기대이득 현실 상한 = **2NN LOO +0.001~+0.011/멤버**(s48=유일 non-kf 백본 +0.011=탈상관 다양성 최대, m2b79=kf-deberta 중복-한정 +0.0014). 시간벽: 1멤버 192s·3멤버 598s(≈203s/멤버) → 4th 동일크기 멤버 추가 시 ~786~800s>600s T4벽 → 큰 카드는 **최약슬롯(m2b79, LOO +0.0014) 교체 or 더 빠른 백본**(blend-w·meta-channel은 챔피언 프레임서 이미 소진, 아래 항목). 감사: results/post_lb-20260715T030000Z-metawave1-1nn.json. **관측 추가(2026-07-15 longshot-dual-0715, n=1 draws — 법칙 아님):** 다양성의 실체는 **레시피 다양성**(seed·아키텍처 단독으론 부족) — metaT 프레임에 동일 레시피 시드추가(k81→metaT_s81) −0.0009373466, 아키텍처 탈상관이나 개별강도 부족한 koelectra(k81→koelectra-base-v3) −0.0039623501, 둘 다 k81(direct-mint 레시피) 유지 미달. 챔피언 구성(s48+k81+metaT79)은 레시피 다양성 관점 국소최적으로 보임(관측, 각 카드 n=1 draw).
- **블렌드가중 재튜닝(frame-specific·챔피언 프레임서 사실상 소진 — 07-15 metatsweep 정정):** metaN 프레임에서는 BLEND_W 0.62>0.6 +0.0010037485(매치드, draw 0)였으나, **챔피언 metaT 프레임 실측 결과 사실상 소진**(argmax=0.62, metaT(0.6) 대비 +0.0000437974 near-zero; w064=−0.0001900624 vs w062로 과튜닝, 0.60–0.62 평탄). metaN 0.62 gain(+0.0010037485)과 metaT 0.62 gain(+0.0000437974)의 ≈23배 격차는 **cross-frame 비교라 frame 교란 포함**(단일 '0.62 스칼라' 효과로 단정 금지) — 다만 **블렌드가중 감도가 frame-specific함(방향뿐 아니라 크기까지)은 확인**. metaT는 BLEND_W=0.6에서 이미 near-optimal. **추가 blend-w 스윕은 챔피언(metaT) 프레임에서 자원 지양** — 남은 목표차는 큰 카드(신규 non-kf 백본)로만. 감사: results/post_lb-20260715T024500Z-metawave1-metatsweep.json.
- **배포기술 축(파킹):** INT8/용량절감 툴링 확보 시 A/B/C 3KF 조합 재개방(전부 밴드 통과했으나 zip 1GB·T4 시간 위반으로 파킹 — §5).
- **공정검증 하네스(전제 인프라)**: logit/prob 계약·동일split(HOLDOUT_IDX=hidx)·멤버별 softmax·밴드=자격필터. 위 카드 판정 기반.

## 7. 인프라 접속 (즉시 사용 가능)
- **치타(무료 A5000×2, 주력):** `ssh -i ~/.ssh/cheetah.pem -p <CHEETAH_PORT> -o StrictHostKeyChecking=no <REMOTE_USER>@<CHEETAH_IP>`. pem은 컨테이너 재시작마다 갱신 필요. 경로 `~/dacon/{env(venv),data,run<name>}`.
  - **학습 런치 패턴:** `cd ~/dacon/run_X && setsid env CUDA_VISIBLE_DEVICES=<0|1> MODEL_NAME=<..> MAX_LEN=<512|768> EPOCHS=6 BATCH=<16|48> GRAD_ACCUM=<1|3> SEED=<n> LR=2e-5 GRAD_CKPT=0 EXTRA_DATA=data/train_mint.jsonl nohup ../env/bin/python -u colab_train_base2.py > train.log 2>&1 < /dev/null & disown`
  - **함정:** kf-deberta/deberta-v3는 GRAD_CKPT=1과 비호환(backward-twice 크래시) → **GRAD_CKPT=0**. kf-deberta batch48 no-ckpt OK, deberta-v3는 batch32, 768은 batch16×accum3. 좀비 프로세스는 `pkill -9 -f colab_train_base2.py`(run명은 cmdline에 없어 매칭안됨! 파일명으로 kill). fp16 변환: `cd ~/dacon && env/bin/python conv_fp16.py <run접미사>`(pack_<X>/model_sub 필요).
  - **재현성 manifest 규칙(07-12 교정10): 전체 printenv 덤프 금지(자격증명 유출 위험).** 학습 스크립트가 파싱한 **effective config JSON**(20개 플래그 전부·기본값 포함 + 코드·데이터 SHA256 + 라이브러리 버전)을 run 디렉터리에 자동 저장(`effective_config.json`, colab_train_base2.py 구현). ±0.001급 차이는 "노이즈" 표현 금지 — "학습 draw·seed·처리 상호작용 범위". matched 실험은 frozen 디렉터리에서 실행.
  - **완료 마커:** `~/dacon/run_X/submit_base2.zip`(refit후). 홀드아웃: 로그의 `홀드아웃 Macro-F1 = X`.
- **엘리스(과금 H100):** 콘솔 <ELICE_CONSOLE_HOST>, `ssh -i ~/.ssh/elice.pem -p <PORT> elicer@<ELICE_TUNNEL_HOST>`(포트 인스턴스마다 변경). ⚠️**끝나면 반드시 인스턴스 종료**. 사전허락 필수.
  - **07-12 승인 운영 규칙:** H100 1대에 **3런 병렬**(4병렬 금지). **레시피 불변**(batch16×accum3·LR·epoch — 하드웨어 이유로 batch 키우면 별도 학습법). frozen code(`cb3ce5d8…`)+effective config 사용. 산출물은 **"H100 training draw"**로 기록(A5000 seed의 정확 재현 아님). 마지막 산출물 SHA 확인 후 **즉시 종료**. 발사: `scratchpad/launch_h100.sh <PORT>` (데이터 번들 `scratchpad/h100_data.tgz` = 치타 중앙 data 136MB).
- **제출은 사용자 직접**(자동화 불가 확정: Chrome확장 10MB제한, 컴퓨터유즈 read-tier). Claude는 zip 포장 + 브리핑(①목적②구성③디테일).

## 8. 파일 지도
- `open/scripts/colab_train_base2.py` — 중앙 학습 스크립트. env: MODEL_NAME/MAX_LEN/EPOCHS/BATCH/GRAD_ACCUM/LR/SEED/GRAD_CKPT/SIBLING_ONLY/EXTRA_DATA/SOFT_TARGET/AU_BOOST/OOF/FGM_EPS(run_fgm판만) 등. HybridNet·extend_positions(상대위치 가드)·make_ds 포함.
- `open/scripts/feat.py` — build_text(고전입력)/build_transcript/build_meta_frame(119메타)/ACTIONS(14클래스순).
- `open/scripts/train.py` — 고전 3모델 학습→`artifacts/model/artifacts.pkl`.
- `open/scripts/mint_data.py` — history 민팅(train_mint.jsonl, +4.5%, 라벨 99.99%).
- `scratchpad/audit_mint_recoverability.py` — read-only mint canonical dedup/메타 exact 복원 전수감사. `--json`으로 재현.
- `open/docs/실험로그.md` — **버전별 스코어보드(전 제출 LB) + 시간순 서사.** 매 제출 갱신.
- `open/docs/model_notes/` — kf_deberta·au_boost·self_distillation·experiments_negative 등 축별 노트.
- `submits/` — 제출 zip(현재 **v49 챔피언**, 앙상블 전제검증 4종 `submit_probe_{2nn_classic,1nn_classic,2nn_only,1nn_only}.zip`, v51/v50/v48 기각 probe, v47 전챔피언). 스테이징은 `scratchpad/probe_*`, `v49/`(챔피언), `v51/`, `v50/`, `v47/`, `v39x/`.
- ⚠️zip 한도는 **1GB decimal(10^9 bytes)** — v47 = 978.6MB, 여유 21.4MB뿐.
- 산출물 계약(감사 §14): `scratchpad/hp_*.npy`류는 **raw logits**(이름과 달리 확률 아님) — 합성 전 멤버별 softmax 필수. sidecar `.meta.json`(split/class order/logits여부/출처)을 병행 생성.
- **`open/coordination/`** — ★2026-07-14 재설계 감사 게이트. **내부 교차검증(Claude 서브에이전트)이 필수 게이트**(구 "독립 감사"→"내부 교차검증"; 세분화 역할 `agent_contracts.md`). **외부 감사(Codex)는 사용자 명시 승인 시에만**, 기본 PREPARE_ONLY·승인 1건당 1회·자동 재시도 없음. ★**교차검증/감사 PASS는 "진행 자격"일 뿐 — 발사·kill·유료GPU·삭제·제출은 각각 별도 사용자 명시 승인**(action_state=WAITING_USER). 러너 `scratchpad/run_codex_gate.py`(prepare/execute 분리, 코어 `codex_gate_lib.py`), 스키마 `schemas/`(request/result/codex-raw/infra_error/usage_ledger v3), 검증기 `validate_audits.py`(legacy=schema_version+backfilled+`legacy_manifest.json`, mtime 아님), 격리테스트 `scratchpad/test_codex_gate.py`(fake Codex), 체크리스트 `checklists/70`. 상세 `open/coordination/README.md` + `agent_contracts.md`.
- 로컬 검증 venv: `open/.venv/bin/python`(transformers 4.57.6). 치타 env는 sklearn 신버전이라 pickle 경고 무해.
- **`open/docs/checklists/`** — ★07-12 강제 검증규약(00 관측오류사전 / 10 학습전 / 20 OOF·증류 / 30 zip생성전 / 40 zip자체 / 50 후보별대조 / 60 제출후). 해당 작업 직전 해당 파일만 읽고 전 항목 점검 — **전 항목 통과만 "검증 완료" 보고 가능.** 자동화: `scratchpad/verify_zip.py` + 5행 E2E. 훅 `.claude/hooks/checklist_guard.py`가 Bash 패턴별 자동 주입.

## 9. 진행 스냅샷 / 큐 (2026-07-12 04:56)
- **★현 챔피언: v58 = 0.7840764953** (챔피언고전×1 + s48 + kf768_79 + kf768_81). v49 대비 `+0.0008921877`, 채점 9분46초.
- **v50 = 0.7807140946, 9분44초 기각.** v47 대비 `−0.0013032848`, v49 대비 `−0.0024702130`. v48과 함께 `train_mint_ci` CI-none화 직접주입 축은 종료하며 v49 유지.
- **카드②(aux79, λ=0.2)는 밴드 탈락으로 미제출.** 4형제 F1은 상승했지만 이 정확한 coarse-aux 구성은 총합 하락; λ 스윕은 보류.
- **v51 = 0.7817138982 기각(14:50 서버).** 이하 제출감사 기록:  `submit_v51.zip` = v49의 `kf768_78 → kf768_80` 단일교체. 978,558,774 bytes, SHA256 `8ba1ef30b5bee2745f7771e58de5379c0b1c84ff454b60a8d552476892149041`; zip·로컬 staging·치타 fp16 pack SHA 일치, full-refit 9,150/9,150 완료, 5행 E2E 3멤버 전원 참여. v51 코어 row F1 0.7670618로 자격 게이트 0.7630 통과(우열 근거 아님). 기존 9분44초/10분 시간 마진 위험은 동일.
- **★pt74 1차는 폐기, clean `pt74c` 진행:** stage A 2,542행은 strict 3,162에서 holdout 세션 620행을 정확히 제외했고 metadata 양채널이 상수/0화되어 누수 없는 gate 조건을 충족한다. stage B는 16:12 시작, 홀드아웃 ETA ≈17:50, 완주 ≈19:40(Claude 감시). 통과 후 full refit은 all-session strict 3,162 stage A부터 별도 재수행.
- **s82는 유지:** 모델 생성 전 `set_seed` 및 env manifest가 적용된 첫 **pipeline-v2+seed82** 후보다. v49/기존 seed와 순수 seed-only 대조는 아니므로 결과를 v2 파이프라인 probe로 기록한다. 다음 clean 대조는 같은 v2의 seed79 재런이 적절하다.
- **⏰ 슬롯:** 오늘(07-12) 5/10 사용, 5 남음. 목표 0.788까지 `+0.0039235047`, 0.7906까지 `+0.0065235047`.
- **사용자 운영 승인(04:29): 현재 치타 run을 중단하고 새 학습으로 교체 가능.** 실행은 Claude 담당, Codex는 계획·검증.
- **신규 스크립트 플래그:** `COARSE_AUX`(4행동군 보조 CE λ, 배포 head 무변경) / `REFIT_ONLY`(홀드아웃 생략 전체재학습 — s80 복구용 ~2h).
- **후속:** pt74c 결과를 우선 확인한다. 그 다음 mint metadata 최대활용은 exact CI+dirty+open strict 교집합 2,863행(holdout 제외 gate 2,314행)에서 119d=0 고정, `[META]` text 유무만 바꾸는 matched A/B로 검증한다. mint2 6,859행은 독립행으로 재주입하지 말고, 시도 시 canonical target별 총가중치 1인 truncation augmentation으로만 별도 probe한다.
- **기본전제 재검증 카드:** v49에서 s48만 제거한 2NN+classic 서버 probe를 우선순위 카드로 추가. 승리/근접하면 1NN+classic으로 축소하고, 신규 강모델은 단독과 v49 교체판을 함께 측정해 단독 품질과 앙상블 기여를 분리한다.
- **앙상블 probe 4종 포장 완료:** `2NN+C` 773,697,274B / `1NN+C` 430,004,849B / `2NN-only` 773,697,274B / `1NN-only` 430,004,837B. 모두 CRC·경로·SHA 대조·5행 E2E 통과. 권장 제출 순서 `2NN+C → 1NN+C → 1NN-only → 2NN-only`; pt74c/exact-meta용 2슬롯 보존.
- **v53 제출 승인:** v49의 k78→pipeline-v2 s82 단일교체. `978,559,539B`, SHA256 `62ad0c...a096028`, 독립 zip/계약/hidx/E2E 검수 통과. v49와 같은 9분44초급 시간마진 위험만 유지.
- **v53 LB 0.7821129829 기각(21:21, 9분47초):** v49 대비 `−0.0010713247`, v47 대비 `+0.0000956035`. s82+pipeline-v2 묶음의 특정 draw가 k78보다 약한 것으로만 해석하며 v49 유지. 오늘 9/10 사용, 마지막 슬롯은 v52(pt74f).
- **v52(pt74f) LB 0.7828474005(9분48초):** 직접 대조 v47 대비 `+0.0008300211`, v49 대비 `−0.0003369071`. strict-dedup mint text-only pretraining→real-only refit의 서버 전달은 검증됐으나 챔피언 교체는 실패. v49 유지, 오늘 10/10 소진.
- **v54(pt79) LB 0.7830942062(9분44초):** 직접 대조 v49 대비 `−0.0000901014`. 동일 seed79의 학습법 단일교체에서 blank-pretext는 사실상 중립에 가까운 소폭 반납이며, v52의 seed74 이득이 강한 k79에 독립적으로 더해지지는 않았다. v49 유지, 오늘(07-12) 1/10 사용. 다음은 자격 통과한 exact-meta keep v55 서버 실측.
- **재현성 교정:** 모델 생성 전 `set_seed(SEED)`와 런치 env manifest는 s82부터 적용됐다. pt74는 그 직전 구버전 런이라 기존 RNG caveat도 유지된다.
- **H100은 보류.** seed 축만으로 남은 +0.0074를 메우기 어려우므로 오늘 GPU 한 장은 반드시 coarse auxiliary/text-only→real-only 같은 큰 카드에 유지.
- **★(19:25 갱신 — 위 보류 문구 대체) H100 과금 승인(18:4x 사용자).** 저녁 8장 개정: ①v57 k74 ②v58 k81 ③v59 s51 ④⑤⑥H100 direct-mint seed79v2/83/84(k79슬롯/k78슬롯 교체) ⑦v56 omit ⑧증류 student(22:30 판단선, fallback: H100 85→k77→pt74c→pt78). 모든 신규 모델은 밴드 통과 후만 제출.
- **v55(pt79k=keep) LB 0.782074495(07:33, 9분44초):** v49 대비 −0.0011098. v49 유지. 오늘 슬롯 2/10 사용(v54·v55), 8 남음.
- **★v57/v58/v59 포장·검증 완료(19:20):** v57=k78→k74(978,558,181B, SHA `2409339653…`), v58=k78→k81(978,557,947B, SHA `14e3227617…`; k81은 manifest 없는 v1 파이프라인 계열 draw로 기록), v59=s48→s51(978,555,873B, SHA `e55e3e98cd…`). 3종 모두 verify_zip 21항 ALL PASS + 5행 E2E(seed참여 3.00·고전단독 0). 사용자 제출 대기.
- **★v57/v58/v59 LB(19:33):** v57 `0.7827231194`(v49 −0.0004612) 기각, **v58 `0.7840764953`(v49 +0.0008922) 신챔피언**, v59 `0.7824698576`(v49 −0.0007145) 기각. 현 프레임은 s48+k79+k81로 갱신. 오늘 5/10 사용.
- **v56(ec79o) 실측 재추정: stage B 18:31 시작 → 완료 21:50~22:10.** OOF79 ≈19:20 완료 예정 → oof74 npy(회수완료)+고전 OOF(`open/artifacts/oof/`)와 matched-T teacher 조립.
- **★07-13 00:15 슬롯 배분(사용자 지시, 리셋 10장):** ①기존조합 probe 3 = A `classic+k79+k81+k78`(순수 3KF, v58 s48→k78) · B `classic+k79+k81+h83`(s48→h83) · C `classic+k79+k81+pt74c`(s48→pt74c) — 모두 신규학습 0, v58 zip 단일-슬롯 스왑. ②balanced-mint seed79·81 = 2 (canonical 3,180 기준·real 중복18 제거·target별 window 총가중=1 or epoch당 target당 window 1샘플·gate holdout mint 완전제외·exact 복원필드만·budget/elapsed·zero-sentinel 금지 → real-only stage B). ③수정 증류 1 (OOF teacher만·split/class order/temp matched·hard CE 유지·s48 슬롯 교체·probe 1장 한정, 밴드통과해도 '증류 성공' 호칭 금지). ④신규 백본 최대 2 (T4단독<4분·fp16/INT8·14-head·1GB zip 게이트 카나리 선통과 후 2seed; Qwen0.5B는 카나리 먼저). ⑤결과기반 후속조합 2 (①②③ LB 보고 조립·예약). **전략 축: keep-시드 증산 비주력, 오류집합을 바꾸는 큰 카드(②④)에 GPU 주력.** ⚠️슬롯10 ≠ GPU2 — 학습 5런(②×2·③·④×2)은 A5000 2레인 직렬화, ①A/B/C는 0-GPU.
- **★07-13 00:35 recon 스냅샷(`open/coordination/snapshots/recon_0713_restart_handoff.md`, 상세·발사커맨드·post-restart 큐):** ⓐ A/B/C는 **"0-GPU 즉시 재포장" 아님** — s48(512-len,압축203.6MB)→kf768(341.2MB) 교체 시 zip ~1,116MB(1GB decimal +116MB 초과)·768-len×3로 9:46/16초 시간마진 붕괴 → 순수 스왑 배포불가(INT8 툴링 없음). 슬롯 전 **로컬 홀드아웃 블렌드 밴드체크로 무료 스크리닝**(§3.4 자격필터). ⓑ 치타 A5000 2장 idle·학습0. ⓒ 백본④: Qwen 폐기(디코더·미캐시·INT8무툴), **`team-lucid/deberta-v3-base-korean` 치타 캐시 존재+인코더(CLS풀링 OK)**로 피벗. ⓓ 증류③: `run_oof74/oof_ids_seed74.json`+`run_oof79/oof_ids_seed79.json` 둘 다 치타 존재(강 teacher 복구가능), 남은 블로커=`make_soft_target.py` temp-match 코드픽스(T=1굽기→v26버그). ⓔ mint2②: 신규 빌더+`TARGET_BALANCE` 가중경로 필요, **`PRETEXT=keep`로 zero-sentinel 원천차단**, ★누수 landmine = leak-guard(:244-248) GroupKFold-fold0 vs gate/hidx split 불일치(1,885중 385만 겹침) → 통일 필수. **주의: 이 절은 recon 실측 증거 기록이며, 축 종결/우열 판정은 서버 LB + Codex post_lb 게이트 후에만.**
- **★07-13 02:00 진행상태(재시작 인수인계):** 서브에이전트 4종(train-launcher·zip-verifier·log-analyst·data-auditor) 등록 확인 완료.
  - **카드① A/B/C 밴드체크(log-analyst) 완료 → 파킹:** 셋 다 in-band Y(A 0.7711/B 0.7751/C 0.7710, gate=v58core−0.0058), 단 s48(512)→kf768(768) 순수 스왑은 zip>1GB+시간마진 붕괴(envelope 위반)·INT8 툴링 없어 **배포불가 → 파킹**(홀드아웃은 서버서열 무예측이라 우열판정 안 함).
  - **★카드② mint2 balanced 발사(seed79=GPU0/seed81=GPU1):** window 다양성+target별 총가중=1. **data-auditor plan_review PASS**(8주장 독립일치, 결과 영속화 `open/coordination/results/plan_review-20260712T170000Z-mint2b.json`). 2-stage: A=`EPOCHS=3 PRETEXT=1 PRETEXT_META=keep TARGET_BALANCE=1 EXTRA_DATA=train_mint2_balanced_gate.jsonl`(5097 window/2314 target) → `./pretext_backbone`; B=`EPOCHS=6 INIT_BACKBONE=./pretext_backbone HOLDOUT_IDX=data/hidx.npy`(real-only). **⚠️최초 02:00 발사는 Stage A를 6ep로 돌린 confound(기존 pretext=3ep) — 사용자 지적으로 중단, 02:14 A=3ep/B=6ep로 재발사(매치드 컨트롤).** 최초 6ep 산출물 폐기. **완주 ETA ≈04:55~05:10 KST**, train-launcher 워처 감시 중. 밴드체크 시 멤버별 softmax·hidx 정렬(신규멤버는 HOLDOUT_IDX=hidx pin으로 자동).
  - **★split-landmine 해소(신규 인프라):** 기존 챔피언 멤버 k79/k81/s48는 전부 **hidx split** 위(k79 F1 hidx정렬 0.7617=recon 일치 / 로컬 GroupKFold-fold0 0.0707 — 로컬 GKF≠hidx, sklearn 버전차, 세션겹침 385/1885). → colab에 **`HOLDOUT_IDX`(고정 홀드아웃 pin)·`TARGET_BALANCE`(target_key별 총가중=1)** 신규 env 추가(colab SHA `415e4c3e`, 치타 업로드됨). 밴드체크·신규 kf 학습은 HOLDOUT_IDX=hidx로 정렬해야 누수0.
  - **빌더:** `scratchpad/build_mint2_balanced.py` → `open/data/train_mint2_balanced{,_gate}.jsonl`(full 6354/2863·gate 5097/2314, exact keep meta·budget/elapsed 제거·gate가 hidx-holdout 세션 완전제외). 배포는 밴드통과 후 full(6354) stage A 별도 재수행 → v58 프레임 kf768→kf768 동급교체(envelope 무위반).
  - **치타 디스크:** 죽은축 29런 22G 삭제(사용자 승인) → 여유 3.1G→25G. 보존: kf768_79/81/78/77·oof74/79(증류 teacher)·pt74c·pretext_ec79o·frozen_ec79.
  - **★카드④ deberta-v3-base-korean prep 완료(02:2x, 학습 미발사):** 계약 카나리 measured — deberta-v2·hidden 768(HybridNet 무변경)·params 134.7M(<kf768)·768-forward OK·fast tokenizer·safetensors 단일. **envelope 통과**(kf768 슬롯 교체 zip ≈886MB/1슬롯·794MB/2seed — dv3ko가 작아 zip 축소, A/B/C와 반대). **verify_zip 일반화 구현**(member-spec 레지스트리 kf768/s48/dv3ko, v58 회귀 27검사 ALL PASS = 하위호환+강화). 배포계획 `open/docs/model_notes/deberta_v3_korean_deploy_prep.md`. **unverified: T4 runtime(estimated ~9:46)·SPM 토크나이저 라운드트립(현행 멤버 전부 WordPiece라 미실증)·dv3ko 멤버 부재.** 발사 순서: mint2 free 후 data-auditor pre_launch → 1seed(MODEL_NAME=team-lucid/deberta-v3-base-korean·768·6ep·16×3·HOLDOUT_IDX=hidx) → 카나리 → 밴드체크 → pre_submit.
  - **★★07-13 10-제출 계획(사용자 확정, 02:4x — 이전 00:15 배분 대체):** 모든 후보 파이프라인 = **data-auditor → 밴드 → full-refit → zip-verifier → 사용자 제출**. A/B/C는 envelope 위반으로 제출목록 제외.
    - **슬롯 구성(사용자 확정): A/B/C 제외·파킹(0) + mint2b ×2 + mint1-exact matched control ×1 + mint2 내부 조건부 ×1 + 신규 백본 ×2 + 증류 ×1 + 결과기반 슬롯·조합 ×3 = 10.**
    1. **mint2b seed79** (balanced 주력, v58 k81 교체) — 밴드 통과 → full 6,354-window Stage A 재수행 후 포장.
    2. **mint2b seed81** (동일 학습법 2nd draw).
    3. **mint1-exact seed79 재대조군** — mint2 rolling-window 자체효과 분리. **keep@2,863, A3/B6, seed79 완전 matched**(1번과 seed/코드/메타 동일, mint_exact→mint2_balanced만 변경). 데이터 = train_mint_exact_gate(2,314).
    4. **mint2b 후속 1장**(조건부): ①3ep가 v58 근접/승 → 같은 seed Stage A **6ep dose A/B** ②약하지만 keep효과 분리 필요 → **mint2-balanced omit@동일행·seed79** ③둘다 저가치 → 신규백본 다른슬롯 교체판.
    5. **신규 DeBERTa-v3 seed79** (v58 k81 교체, prep 완료·카나리 통과).
    6. **신규 DeBERTa-v3 seed81** (draw 분산).
    7. **수정 증류 student** (v58 s48 교체) — 조건: **temperature/split/id/class-order 감사 PASS**. teacher = oof74/79 + classic OOF.
    8. **당일 최고 신규모델 k79 교체판** (기본 k81교체 vs k79교체 슬롯 상호작용, 1~7 결과기반).
    9. **당일 신규모델 최강 2종 조합** = s48 + newA + newB + classic (k79/k81 모두 대체, envelope 통과 필수).
    10. **최강 신규모델 1NN+classic 진단** (단독품질 vs 앙상블 한계효과).
    - **실행순서:** mint2b gate 완료(진행중) → 밴드통과팔 full A3→refit B6; **동시 CPU: 신규백본 카나리(완료)·증류 teacher 준비** → mint2 full-refit 후 GPU 신규백본 2seed → 남는 GPU에 matched mint1(카드3)·증류 student → 서버결과 순서로 8~10 조합 결정.
  - **★CPU prep 상태(02:5x, GPU는 mint2b 점유):** **카드7 증류 teacher 준비 완료** — `make_soft_target.py` temperature-matched 재작성(softmax(logits/T), logits/probs 혼합처리, 공통 id 교집합, T를 npz 저장) + colab이 `DISTILL_T==npz T` assert(v26 버그 코드차단, 새 colab SHA `44aa810b`, SOFT_TARGET 시에만 작동→mint2b/card3/dv3ko 무영향). oof74/79 logits+ids·classic OOF 회수, 조립 dry-run = **공통 id 70000 real·T=2·확률합1·finite**(real-only student 커버). **미검증(카드7 data-auditor 게이트): session분리/in-sample·ACTIONS class-order 3teacher 정렬·커버리지=student 학습셋·s48 슬롯계약.** ⑤결과기반 후속.
  - **카드3 데이터 확인:** train_mint_exact_gate(2,314행·exact-keep meta가 mint2b와 동일·budget/elapsed 0). 완전matched 위해 target_key 필드만 추가하면 mint2b와 동일 체인(A3/B6 TARGET_BALANCE) — 발사 시 추가.
  - **★★07-13 22:34 LB 결과 (카드1·2·5·6 제출, post_lb PASS):** **카드1 mint2b_s79 = 0.784156489 → 신챔피언(v58 +0.0000799937 결정론적 슬림 갱신).** 카드2 mint2b_s81 −0.00141 / 카드5 dv3ko_s79 −0.00663 / 카드6 dv3ko_s81 −0.00528(전부 v58 미달). 시간 9:29~9:47. **해석(감사 규율):** mint2b는 seed79 draw 안착·seed81 −0.00141로 **seed 드로우 의존**(margin이 seed 스프레드 내), balanced 자체효과는 카드3 대조군 미귀환이라 미분리. dv3ko는 홀드 in-band였으나 LB 미달 = **홀드아웃 무예측 재확인**, recipe-matched v2 백본 draw가 v58 k81 미달(확장보류). 결과 `open/coordination/results/post_lb-20260713T133500Z-round1.json`. 오늘 슬롯 4/10.
  - **밤샘 진행:** 카드3(mint1-exact 대조군, seed79·81) 학습중→밴드체크 예정 / 증류(카드7, plan_review PASS·발사대기)→카드3 GPU free 시 발사. 카드4(Stage A 6ep dose): 카드1 승으로 트리거 형식충족이나 "seed79 draw 한정 근접·dose는 draw 분산 탐색" 프레이밍. dv3ko 2seed 미달로 확장 재고.
  - **★07-14 00:15 밤샘 GPU 큐(사용자 지시: "계획 세워 문서화하고 내일 확인 전까지 이행") — 카드3 23:41 완주(GPU 2장 free), 워처 보고 홀드 mint1x_s79 0.7609 / s81 0.7477(단일멤버, log-analyst 재검증 중):**
    - **Lane A(GPU0) 카드7 증류 s48+KD probe: ✅완결(03:2x) — 아침 제출대기 1호.** 00:07 발사→02:17 완주(단일멤버 홀드 0.7521)→밴드 in-band Y(probe 블렌드 0.775027 ≥ 하한 0.767910, 챔피언 재현 0.773710)→fp16 포장 `submits/submit_distill.zip`(982,200,110B, SHA c450aeeb…)→zip-verifier(verify_zip 27/27·SHA체인 8/8·E2E seed 3.00)→**pre_submit 감사 PASS**(results/pre_submit-20260713T180500Z-distill.json). runtime=estimated(챔피언 동구조 512-len 스왑). probe 1장 한정·'증류 성공' 호칭 금지. KD 델타 +0.0013(카드1 프레임 홀드 관찰치, 코드버전차 confound라 순수 A/B 아님). GPU0 free.
    - **Lane B 카드4 dose6: gate 완결 + 배포 체인 진행중.** ①gate chain(run_mint2b_dose6_s79, 00:21 발사): pre_launch PASS·G1~G5 통과(root colab 드리프트 cb3ce5d8 지뢰 회피, G5 diff 유일차이=EPOCHS 3→6), 04:07 완주. 단일멤버 홀드 0.7590(카드1 0.7650 대비 −0.0060 관찰치). **②밴드체크 in-band Y**(probe 블렌드 s48+k79+dose6 = 0.773437 ≥ 하한 0.767910, margin +0.005527; 블렌드 델타 −0.000274 = 사실상 평평). ③**배포 체인(run_mint2bdep_dose6_s79, 04:23 발사, GPU0):** 별도 pre_launch PASS(카드1 배포 실측 = FULL 6,354 a8e7e7d2 Stage A + REFIT_ONLY Stage B 무홀드아웃 70k refit) → G1~G6 통과, Stage A 완료 04:31. **완주 ETA ≈06:20~06:35 KST(백스톱 08:00)** → fp16 포장 → zip-verifier → pre_submit → 아침 제출대기 2호. 프레이밍 = "dose draw 분산 탐색"(게이트 홀드는 개선 아님 — 단정 금지, LB는 card1 배포 대비로만 해석). 감사 2건: results/pre_launch-20260713T151500Z-card4dose.json·pre_launch-20260713T193500Z-card4dose-deploy.json.
    - **밴드 탈락 시:** 해당 lane 파킹, GPU는 아침 사용자 판단 대기(keep-시드 증산 자동발사 금지 — 비주력 원칙). 중단·폐기는 비홀드아웃 근거로만.
    - **카드3 후속:** log-analyst 밴드체크+matched-control 델타(rolling vs mint1-exact, seed별) → data-auditor 검토 → 실험로그 append. 우열선언 없음(홀드 무예측).
    - **아침 산출물 목표:** 증류 zip(조건부)·카드4 zip(조건부·refit 완료시)·cA~cF 재업로드 대기(07-13 자정 만료로 cC~cF 미제출, cA/cB 링크도 1h 만료 — 오늘 슬롯 10장에 재업로드 필요).
  - **★★07-14 07:0x 밤샘 큐 완결(전 게이트 통과):** **제출대기 2장** — ①카드7 `submits/submit_distill.zip`(982,200,110B, c450aeeb…, 밴드 0.775027 in-band Y, pre_submit PASS) ②카드4 `submits/submit_dose6.zip`(982,197,615B, 5cb5ba54…, 밴드 0.773437 in-band Y, pre_submit PASS). 둘 다 runtime=**estimated**(챔피언 동구조 스왑·서버 미측정)·probe 1장 한정·'성공/우위' 호칭 금지. 카드3 대조군 완결(rolling−exact 델타 +0.0041/+0.0057, seed 스프레드 내). **치타 GPU 2장 free·자동발사 없음.** 오늘(07-14) 슬롯 10장 후보 풀 = 신규 2장 + cA~cF 6장(zip 보유, 재업로드만). 감사 영속화 7건(plan_review 1·pre_launch 3·pre_submit 3) + 실험로그 append 완료. **다음: 사용자 제출 순서 결정 → 제출 → post_lb 감사 → 기록.**
  - **★★07-14 아침 갱신: cA = 0.7848461512 신챔피언**(07-13 23:52~53 사용자가 cA·cB 제출 성공 — 어제 6/10). cB 0.781756698 기각. post_lb round2 PASS(results/post_lb-20260714T000000Z-round2.json). §1 갱신됨. 스테이징 오염(§1) — 조합 재zip 금지.
  - **★07-14 10:2x round3(post_lb PASS): distill·dose6 probe 둘 다 기각, cA 유지. 오늘 2/10.** dose6 0.7837566237(카드1 −0.0004, matched 1변수 — draw 분산 내, **dose 무확장**) / distill 0.7822362774(카드1 −0.0019, confounded — **증류축 전략수준 종료**, v26 death 재확인). **홀드 무예측 직접 반례 추가**(distill 홀드 +0.0013 → LB −0.0019 부호반전). 잔여 후보 감사판정: cA프레임 재포장 폐기(기대가치 낮음)·cC~cF 하향 prior(투입은 사용자 전략)·**mint2b 74·78 = 비주력 값싼 병행 gate로만(자동발사 금지, 사용자 승인 대기)**. 남은 결과기반 0-GPU 카드: cA 프레임 제3슬롯 k-스왑(k74/77/78/80 기존멤버, 로컬 밴드 스크리닝 무료) — 미감사·미실행. 감사: results/post_lb-20260714T014500Z-round3.json.
  - **★★07-14 18:5x campaign mint2v2-dualseed-0714 완주 + LB(post_lb PASS): 둘 다 cA 미달, 챔피언 cA 유지.** `submit_m2v2s79.zip` LB 0.7831437144(9:48) / `submit_m2v2s81.zip` LB 0.7844367012(9:48) — cA(0.7848461512) 프레임에서 mint2b_s79 슬롯 단일교체(배포 full refit). delta: s79−cA −0.0017024368 / s81−cA −0.0004094500 / s81−s79 +0.0012929868(seed 스프레드 내 — 'v2 seed81 우위' 호칭 금지). matched(seed79 공유, 변수=v2 4묶음: rolling-window strict + TB_V2 배치독립 가중손실 + target가중 클래스가중 + 메타 omit) — n=1·묶음이라 **'v2 축 죽음/단독효과' 단정 금지**. 홀드 BAND_EVAL(s79 0.770386 > s81 0.768503) vs LB(s81 > s79) = **순위 반전** 재확인(홀드 무예측). runtime 588s(9:48)<600s → **measured** 승격(마진 12s, 얇음). **오늘(07-14) 슬롯 누적 4/10**(round3 distill·dose6 2건 + 본 캠페인 2건). **치타 GPU 2장 idle**, campaign run(`run_mint2v2b_s79`/`run_mint2v2_s81_gate` 등)·pack 디렉터리는 보존(자동삭제 없음, 삭제는 사용자 지시로만). 감사: results/post_lb-20260714T101500Z-mint2v2-dualseed.json.
  - **★★07-14 20:1x campaign official-meta-wave1 Part A: cA 2NN+C ablation 3종 LB(post_lb PASS), 챔피언 cA 불변.** 멤버 leave-one-out 서버실측(각 cA에서 seed멤버 1개 제거, 나머지 2seed+classic 고정): drop_s48=0.7737917529(6:28) / drop_k81=0.7796416557(6:12) / drop_m2b79=0.7834225454(6:17), 전부 cA(0.7848461512) 미달 → 제거는 손해, cA 유지(2NN은 진단용). **멤버 LOO 한계기여 랭킹: s48 +0.0110543983 ≫ k81 +0.0052044955 > m2b79 +0.0014236058**(s48이 유일 non-kf-deberta 백본 → 다양성 레버 시사, m2b79는 k81과 중복이라 한계기여 최저). **프레이밍 규율: m2b79 +0.0014는 그 슬롯 교체(mint2v2·META)의 상한 캡이 아니라 현재멤버의 중복-한정 LOO** — 동일계열(kf-deberta) 교체는 상승 제한, 더 탈상관 교체는 초과 가능. LOO 델타 = 개별품질+다양성기여 혼합(비가법) — '멤버 자체 품질' 단독 오독 금지. runtime 6:12~6:28 measured(2멤버라 여유, 3멤버 후보 런타임 증명 아님). 전략 함의: 남은 목표차 +0.0082238488의 최대 레버 = 신규 non-kf-deberta 백본(다양성). 감사: results/post_lb-20260714T112000Z-metawave1-2nn.json.
  - **★07-14 슬롯 확정: 오늘 누적 7/10 사용**(round3 distill·dose6 2건 + campaign mint2v2-dualseed 2건 + 본 2NN ablation 3건). §1 챔피언 cA(0.7848461512) 불변.
  - **META-N/T 학습 중(치타 GPU):** m2b79 슬롯 교체 후보(k81과 동일계열 kf-deberta) — 위 LOO 랭킹상 동일계열 교체는 중복-한정으로 상승폭 제한 예상(META 포함), 결과는 별도 post_lb에서 판정.
  - **★★07-14 밤 campaign official-meta-wave1 Part B 완주(post_lb PASS): ★챔피언 교체 cA→META-T.** submit_metaN.zip 0.7850069558(23:27, 584s) / submit_metaT.zip 0.7866536897(23:40, 598s<600). metaN−cA +0.0001608046(약) / metaT−cA +0.0018075385(draw폭 초과 → 신챔피언 자동채택) / metaT−metaN +0.0016467339(시사·강단정 유보, n=1). **transcript-meta 정보축 서버전달 확인**(§6). ⚠️metaT 598s/600s 마진 2s — 향후 metaT 프레임 시간벽 리스크. 목표차: META-T→12등(0.79307) = +0.0064163103. 감사: results/post_lb-20260714T150000Z-metawave1-metant.json. 마지막 슬롯 예정: submit_metaN_w062.zip(META-N + BLEND_W 0.62, build-sanity만) — 진 축 기반이라 기대이득 낮음. **슬롯: 07-14 소진(리셋 후 07-15 신규, 예선 마감 7/15).**
  - **★★07-15 01:3x campaign official-meta-wave1 후속 LB(post_lb PASS): metaN_w062 0.7860107043(BLEND_W 0.62, +0.0010037485 matched vs metaN 0.6) — 챔피언 metaT(0.7866536897) 불변.** BLEND_W 0.62>0.6이 meta 증강 프레임에서 매치드 1변수(draw 0) +0.0010 → §5 블렌드w 죽은축을 frame-specific으로 정정, §6에 시간중립 레버로 재등재. metaT 프레임 0.62/0.64 스윕은 빌드만 완료(SHA 2bdd24e9/32cd1bfd), 제출대기. 목표차 챔피언 metaT→12등(0.79307) +0.0064163103 불변. 슬롯: 07-15(자정 리셋 신규). 감사: results/post_lb-20260715T013000Z-metawave1-blendw.json.
  - **★★07-15 02:4x campaign official-meta-wave1 후속 metaT 프레임 BLEND_W 스윕 LB(post_lb PASS): ★챔피언 교체 META-T(0.60)→META-T_w062(0.62).** submit_metaT.zip(0.60)=0.7866536897(598s) / submit_metaT_w062.zip(0.62)=**0.7866974871**(600s, 최고LB→자동채택 신챔피언) / submit_metaT_w064.zip(0.64)=0.7865074247(599s). delta: w062−metaT(0.6)=+0.0000437974(near-zero) / w064−metaT(0.6)=−0.0001462650 / w062−w064=+0.0001900624. **within-frame matched(멤버·transcript 불변, BLEND_W 단일 스칼라, 재학습 없음·draw0)** — argmax=0.62, 0.60–0.62 평탄, 0.64 과튜닝. **직전 blendw 레코드의 'metaT 0.62 적용 시 +0.0010 전이·~0.7877 신챔피언' 가설을 실측 반증**(0.7866974871 ≪ 0.7877; metaN 0.62 gain +0.0010037485 vs metaT 0.62 gain +0.0000437974 ≈23배 작음, 단 cross-frame 교란 포함). **블렌드w 레버는 챔피언 프레임에서 사실상 소진**(§6 정정) — 추가 스윕 자원 지양, 주력은 큰 카드(신규 non-kf 백본). runtime measured 600/599/598s, 마진≈0(w062가 정각) — metaT 프레임 추가 멤버·대형화 여전히 불가. **목표차(일관쌍, ref 12등 0.79307 변동가능 기준): 챔피언 metaT_w062(0.7866974871)→+0.0063725129.** 슬롯: 07-15(자정 리셋). 감사: results/post_lb-20260715T024500Z-metawave1-metatsweep.json.
  - **★★07-15 campaign all-info-h100-final-0715 post_lb PASS: negative/미채택, 챔피언 metaT_w062(0.7866974871) 불변.** submit_allinfo_3nn.zip=0.7850171837(598s) / submit_allinfo_1nnc.zip=0.7318299466(191s) — 3nn vs 챔피언 −0.0016803034, metaN(0.7850069558)과 사실상 동률(+0.0000102279). **2슬롯 소진(3nn·1nnc), 순수 1nn(BLEND_W=1.0) ALL-INFO 단독은 미제출**(unverified_claims). 잔여 목표차 +0.0063725129(12등 ref 0.79307 기준, 리더보드 변동값 — 갱신 없음, 챔피언 불변). **인프라: Elice H100 터널 NO_ACTIVE_TARGET 사고로 A100 draw 전환, 유료 GPU 산출물 SHA 확인 후 종료 확인(사용자).** 명칭정정('ALL-INFO'→'ALL-REAL+STABLE-MINT')·감사결함 4건(Stage A effective_config 덮어쓰기/harvest SHA256SUMS.txt fp32 계보오류/pre_submit 감사 소급영속화/BLEND_W 0.60≠0.62 프레이밍) 정정 기록. **다음 큰 카드 후보:** presence-masked mint 최대활용(stable5+exact2863 CI/dirty/open 마스크 동반) 또는 신규 non-kf 백본. 상세·감사: 실험로그 2026-07-15 항목, results/post_lb-20260715T090000Z-allinfo-3nn1nnc.json.
  - **★07-15 s48+ALLINFO+C 2NN 진단 LB: 0.7810(k81 가치 −0.004 재확인, 챔피언 metaT_w062 불변) + campaign longshot-dual-0715 발사(치타 A5000×2, Card A=metaT_s81 / Card B=koelectra-base-v3), ETA ~08:00-08:40 완주 예상(마감 10:00).** 상세: 실험로그 2026-07-15 항목.
  - **★07-15 campaign longshot-dual-0715 서버 실측 완료(post_lb): Card B koelectra-base-v3(k81 슬롯 교체) = 0.7827351370(578s), Card A metaT_s81(k81 슬롯 교체) = 0.7857601405(598s) — 둘 다 챔피언 metaT_w062(0.7866974871) 미달(−0.0039623501 / −0.0009373466).**
  - **해석(관측, n=1 draws): k81의 가치는 시드가 아니라 레시피-다양성** — 동일레시피(metaT) 시드추가 −0.0009 < 아키텍처탈상관(koel) −0.0040 미달폭, 챔피언 구성(s48+k81+metaT79)은 레시피 다양성 관점 국소최적으로 보임.
  - **submit_seedlotto_w062.zip(챔피언 프레임 동일·metaT 멤버 seed79→81 단일교체, matched) 빌드·E2E 통과, 사용자 제출 대기.**
  - **잔여 슬롯 2/10(오늘 07-15). 챔피언 metaT_w062 0.7866974871 불변.**
  - **★예선 종료(2026-07-15 10:00 마감, 슬롯 전량 소진): 최종 2제출 = seedlotto_w062 0.7866826118(599s, vs챔피언 −0.0000148753) / wmean_w062 0.786608523(598s, vs챔피언 −0.0000889641), 둘 다 챔피언 미달.** 최종 챔피언 metaT_w062 0.7866974871 확정, 본선 컷 0.79591 대비 −0.0092 미달 → 본선 진출 실패. 상세: 실험로그 2026-07-15 "예선 종료" 항목.

## 10. 📌 갱신 프로토콜 (하네스)
새 채팅/에이전트는 반드시:
1. **시작 시:** 이 HANDOFF.md + `실험로그.md` 읽기. §7로 인프라 상태 확인(nvidia-smi, 실행중 run).
2. **매 제출 후:** `실험로그.md` 스코어보드 행 추가 + §1(챔피언)·§9(스냅샷) 갱신.
2b. **★zip 만들 때마다:** 별도 제출메모 파일 금지 — `실험로그.md`에 통합 기록(①목적②구성③디테일, 제출 후 ④LB). 그와 **별개로** 사이트 붙여넣기용 짧은 메모를 채팅으로 사용자에게 제공(사용자가 Dacon 사이트에 직접 붙여넣음). zip 내 memo.txt 불필요.
2c. **★실험 돌릴 때마다 예상 완료 시각(ETA) 명시.** **사용자가 규칙 말하면 즉시 CLAUDE.md에 추가.**
3. **새 법칙/축 판정 시:** §3(법칙)·§5(죽은축)·§6(살아있는축) 갱신.
4. **인프라 변경 시:** §7 갱신.
5. 메모리(`dacon-236694-agent-action.md`)도 병행 갱신하되, **이 파일이 우선 진실 소스**(메모리 소실 대비).
6. 작업방식 선호: epoch별 알림 금지·완료시 소요보고, GPU클라우드(엘리스) 사전허락, 제출은 사용자직접, 모델노트 의무, 파일삭제는 zsh 안전방식(변수 whitelist 금지 — 단어분리 안 돼 전삭제 사고 있었음).
